machine:
  java:
    version: openjdk7

dependencies:
  pre:
    - ./gradle
  override:
    - ./gradlew dependencies

compile:
  override:
    - ./gradlew assemble

test:
  override:
    - ./gradlew test
